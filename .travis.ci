dist: trusty
sudo: false
language: node_js

# Set node_js builder version to latest version https://docs.travis-ci.com/user/languages/javascript-with-nodejs/
node_js:
  - node

# Run before running the real script (early loader)
before_script:
  - npm install -g gulp-cli

# Run normally as the real script
script: 
  - gulp
